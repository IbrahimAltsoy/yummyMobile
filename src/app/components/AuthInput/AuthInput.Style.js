import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  container: {
    width: "100%",
  },
  input: {
    height: 50,
    borderColor: "gray",
    borderWidth: 1,
    marginBottom: 12,
    paddingLeft: 8,
    fontSize: 16,
    borderRadius: 5,
    backgroundColor: "#ffffff",
    color: "#000", // Yazı rengini siyah olarak ayarlayın
  },
});
export default styles;
